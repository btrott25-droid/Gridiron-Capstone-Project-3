# Tested with Python 3.10.20.
# Version ranges are intentionally flexible enough to recreate the notebook
# while avoiding unnecessary environment-specific pins.

numpy>=1.24,<3
pandas>=2.0,<3
matplotlib>=3.7,<4
torch>=2.0
transformers>=4.40,<6
datasets>=2.14,<5
accelerate>=0.30,<2
evaluate>=0.4,<1
rouge-score>=0.1.2,<1
ipykernel>=6.25,<8
ipywidgets>=8.0,<9
